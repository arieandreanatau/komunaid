{
  "outputDirectory": "public",
  "framework": null,
  "buildCommand": "composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist && npm ci --no-audit --no-fund && npm run build && php artisan package:discover --ansi && php artisan view:cache --ansi && php artisan config:cache --ansi && php artisan route:cache --ansi",
  "installCommand": "echo 'composer install handled in buildCommand'",
  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.8.0",
      "maxDuration": 60
    },
    "api/cron/scheduler.php": {
      "runtime": "vercel-php@0.8.0",
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.php"
    }
  ],
  "crons": [
    {
      "path": "/api/cron/scheduler?token=__CRON_SECRET__",
      "schedule": "0 0 * * *"
    }
  ],
  "regions": ["sin1"]
}

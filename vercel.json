{
  "outputDirectory": "public",
  "framework": null,
  "buildCommand": "npm run build",
  "installCommand": "curl -sS https://getcomposer.org/installer | php -- --install-dir=/tmp --filename=composer && export PATH=\"/tmp:$PATH\" && composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist && php artisan package:discover --ansi && php artisan view:cache --ansi && php artisan config:cache --ansi && php artisan route:cache --ansi",
  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.8.0",
      "maxDuration": 60
    },
    "api/cron/scheduler.php": {
      "runtime": "vercel-php@0.8.0",
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.php"
    }
  ],
  "crons": [
    {
      "path": "/api/cron/scheduler?token=__CRON_SECRET__",
      "schedule": "0 0 * * *"
    }
  ],
  "regions": ["sin1"]
}

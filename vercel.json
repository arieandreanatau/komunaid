{
  "outputDirectory": "public",
  "framework": null,
  "buildCommand": "npm run build",
  "installCommand": "npx -p composer/composer@2.7.7 composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist && npx -p php/php -y php artisan view:cache && npx -p php/php -y php artisan config:cache && npx -p php/php -y php artisan route:cache",
  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.8.0",
      "maxDuration": 60
    },
    "api/cron/scheduler.php": {
      "runtime": "vercel-php@0.8.0",
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.php"
    }
  ],
  "crons": [
    {
      "path": "/api/cron/scheduler?token=__CRON_SECRET__",
      "schedule": "0 0 * * *"
    }
  ],
  "regions": ["sin1"]
}

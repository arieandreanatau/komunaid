# KomunaID V2 — Production Environment Template
# Copy this to .env on production server and fill in ALL values
# NEVER commit this file to repository
# NEVER share this file

APP_NAME=KomunaID
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://your-domain.com

APP_LOCALE=id
APP_FALLBACK_LOCALE=id
APP_FAKER_LOCALE=id_ID

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=komunaid_production
DB_USERNAME=your_production_db_user
DB_PASSWORD=your_production_db_password

CACHE_STORE=database
QUEUE_CONNECTION=database
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=your-domain.com
SESSION_SECURE_COOKIE=true

FILESYSTEM_DISK=public
PUBLIC_DISK_DRIVER=local
PUBLIC_DISK_URL="${APP_URL}/storage"

MAIL_MAILER=smtp
MAIL_HOST=smtp.mailgun.org
MAIL_PORT=587
MAIL_USERNAME=your_mail_username
MAIL_PASSWORD=your_mail_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=no-reply@your-domain.com
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"

# ============================================
# PRODUCTION SECURITY NOTES:
# ============================================
# 1. Generate APP_KEY: php artisan key:generate
# 2. APP_DEBUG MUST be false
# 3. APP_ENV MUST be production
# 4. DB_PASSWORD must be strong (16+ chars)
# 5. Don't use root DB user if possible
# 6. HTTPS is MANDATORY
# 7. SESSION_SECURE_COOKIE=true for HTTPS
# 8. LOG_LEVEL=error (not debug)
# 9. Never commit this file
# 10. Never expose this file publicly
#
# For Cloudflare R2 storage in production:
# PUBLIC_DISK_DRIVER=s3
# R2_ACCESS_KEY_ID=your-key
# R2_SECRET_ACCESS_KEY=your-secret
# R2_BUCKET=your-bucket
# R2_ENDPOINT=https://your-account.r2.cloudflarestorage.com
# PUBLIC_DISK_URL=https://pub-xxxx.r2.dev
# ============================================

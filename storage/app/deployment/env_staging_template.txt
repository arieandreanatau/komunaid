# KomunaID V2 — Staging Environment Template
# Copy this to .env on staging server and fill in values
# NEVER commit filled version to repository

APP_NAME=KomunaID
APP_ENV=staging
APP_KEY=
APP_DEBUG=true
APP_URL=https://staging.your-domain.com

APP_LOCALE=id
APP_FALLBACK_LOCALE=id
APP_FAKER_LOCALE=id_ID

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=komunaid_staging
DB_USERNAME=your_staging_db_user
DB_PASSWORD=your_staging_db_password

CACHE_STORE=file
QUEUE_CONNECTION=database
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=staging.your-domain.com

FILESYSTEM_DISK=public
PUBLIC_DISK_DRIVER=local
PUBLIC_DISK_URL="${APP_URL}/storage"

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=no-reply@your-domain.com
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"

# SECURITY NOTES:
# - APP_DEBUG=true ONLY if staging is private
# - If staging is public, set APP_DEBUG=false
# - Use separate database from production
# - Never use production credentials
# - Enable HTTPS if staging is public
